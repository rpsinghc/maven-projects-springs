<?xml version="1.0" encoding="ISO-8859-1" ?>
<ServerInfo xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
    <!--         xsi:noNamespaceSchemaLocation="./resources/common/ServerConfig.xsd"
                 The schema validation happens on the DOM tree in the code.
    -->
    <!--
        name - indicates the machine name in which the servers are running.
        Alias - Alias name for the server. Must be unique within this xml file.
        Url - The URL used for connection to the configured server. The tag value for this tag may contain
            multiple URLs separated by comma or any other appropriate separator as a single String value.
        SuperUser
            password - SuperUser password that is configured in the target server
            encrypted - indicates whether the value of the password is encrypted or not. The password can be encrypted using encrypt.cmd command from the bin directory in installation.
            The text for the SuperUser tag should be the name of the SuperUser.
        Description - Description of the server configuration
        Category - Category under which the configured server belongs to.
        Properties - Any custom properties as name-value pair can be defined
            here. The keys should be unique among themselves.

        Sample:
            <Server name="machine-name">
               <Alias>alias-name</Alias>
               <Url>http://198.168.1.1:18001</Url>
               <SuperUser password="password" encrypted="false">username</SuperUser>
               <Description>Description</Description>
               <Category>category</Category>
               <Properties>
                  <Property key="key1" value="value1"/>
                  <Property key="key2" value="value2"/>
               </Properties>
            </Server>
     -->
</ServerInfo>
